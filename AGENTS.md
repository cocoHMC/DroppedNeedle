# DroppedNeedle

Use `README.md` and the repository’s manifests for setup and checks. For Kyle’s CT101 deployment, preserve the existing Lidarr service until Kyle accepts its replacement. Native library scanning is read-only; keep Library Management disabled until its media-write plan is separately reviewed. Test scans must not modify Plex media.

## Shared infrastructure

On Kyle’s hosts, follow `/root/AGENTS.md` (Hermes) or `/Users/kyle_wils/AGENTS.md` (Mac). Read linked platform procedures only when that work applies.
